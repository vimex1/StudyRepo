# StudyRepo

Образовательная платформа с системой хранения заданий и решений для студентов.

## 🚀 Технологии

### Backend
- **Python** 3.11+
- **Django** 4.2.24
- **Django Ninja** - современный веб-фреймворк для создания API
- **PostgreSQL** 15+ - база данных
- **Poetry** - управление зависимостями Python

### Frontend
- **React** 19.1.1
- **TypeScript** 5.9.3
- **Vite** 7.1.7 - сборщик и dev-сервер
- **React Router** 7.9.3 - маршрутизация
- **Nginx** - веб-сервер для production

### Инфраструктура
- **Docker** 24.0+ и **Docker Compose** - контейнеризация
- **Make** - упрощение управления (опционально)

## 📋 Требования

Для запуска проекта вам понадобится:

- **Docker** и **Docker Compose** ([установка Docker](https://docs.docker.com/get-docker/))
- **Poetry** ([установка Poetry](https://python-poetry.org/docs/#installing-with-the-official-installer)) - для локальной разработки
- **Node.js** 20+ и **npm** - для локальной разработки фронтенда (опционально)
- **Python** 3.11+ - для локальной разработки бэкенда (опционально)
- **PostgreSQL** 15+ - для локальной разработки (опционально, если не используете Docker)

## 🏃 Быстрый старт с Docker

Самый простой способ запустить проект - использовать Docker Compose:

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/vladislav1029/StudyRepo
cd StudyRepo
```

### 2. Создайте файл `.env` (опционально)

Создайте файл `.env` в корне проекта для настройки переменных окружения:

```env
# Database Configuration
POSTGRES_DB=studyrepo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:80
DEBUG=True

# Ports
BACKEND_PORT=8000
FRONTEND_PORT=3000
```

> **Примечание:** Если файл `.env` не создан, будут использованы значения по умолчанию из `docker-compose.yml`.

### 3. Запустите проект

**Вариант 1: Используя Make (рекомендуется)**

```bash
make start
```

**Вариант 2: Используя Docker Compose напрямую**

```bash
docker compose up -d --build
```

Эта команда:
- Соберет образы для бэкенда и фронтенда
- Запустит PostgreSQL базу данных
- Применит миграции Django
- Запустит все сервисы

### 4. Создайте суперпользователя Django

```bash
docker compose exec backend python manage.py createsuperuser
```

Следуйте инструкциям для создания администратора.

### 5. Доступ к приложению

После запуска проект будет доступен по следующим адресам:

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **Django Admin:** http://localhost:8000/admin/
- **pgAdmin (PostgreSQL):** http://localhost:5050
  - Email: `admin@admin.com` (по умолчанию)
  - Password: `admin` (по умолчанию)
- **API Docs:** http://localhost:8000/api/docs (если настроено)

## 💻 Локальная разработка

Если вы хотите разрабатывать локально без Docker:

### Backend (Django)

#### 1. Установите зависимости с помощью Poetry

```bash
cd backend
poetry install
```

#### 2. Активируйте виртуальное окружение

```bash
poetry shell
```

#### 3. Настройте базу данных

Создайте файл `.env` в директории `backend/`:

```env
POSTGRES_DB=studyrepo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DJANGO_SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
DEBUG=True
```

#### 4. Примените миграции

```bash
python manage.py migrate
```

#### 5. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

#### 6. Запустите сервер разработки

```bash
python manage.py runserver
```

Backend будет доступен на http://localhost:8000

### Frontend (React)

#### 1. Установите зависимости

```bash
cd frontend
npm install
```

#### 2. Запустите dev-сервер

```bash
npm run dev
```

Frontend будет доступен на http://localhost:5173 (или другой порт, указанный Vite)

#### 3. Сборка для production

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

## 📁 Структура проекта

```
StudyRepo/
├── backend/                 # Django backend
│   ├── core/
│   │   ├── apps/
│   │   │   ├── labs/       # Приложение для лабораторных работ
│   │   │   └── users/       # Приложение для пользователей
│   │   └── project/        # Настройки Django проекта
│   ├── Dockerfile          # Docker образ для backend
│   ├── manage.py
│   ├── pyproject.toml      # Зависимости Poetry
│   └── poetry.lock
├── frontend/               # React frontend
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   └── ...
│   ├── Dockerfile         # Docker образ для frontend
│   ├── nginx.conf         # Конфигурация Nginx
│   └── package.json
└── docker-compose.yml     # Оркестрация всех сервисов
```

## 🔌 API Endpoints

### Основные эндпоинты:

- `GET /` - Информация об API
- `GET /admin/` - Django Admin панель
- `/api/auth/` - API аутентификации
- `/api/labs/` - API лабораторных работ

### Документация API

После запуска проекта, документация API доступна по адресу:
- Swagger UI: http://localhost:8000/api/docs
- ReDoc: http://localhost:8000/api/redoc

## 🛠️ Полезные команды

### Make (упрощенные команды)

Если у вас установлен Make, вы можете использовать упрощенные команды:

```bash
make help              # Показать все доступные команды
make start             # Полный запуск проекта (сборка, запуск, миграции)
make up                # Запустить все контейнеры
make down              # Остановить все контейнеры
make restart           # Перезапустить все контейнеры
make logs              # Показать логи всех сервисов
make logs-backend      # Показать логи бэкенда
make logs-frontend     # Показать логи фронтенда
make shell             # Открыть shell в контейнере бэкенда
make migrate           # Применить миграции Django
make makemigrations    # Создать миграции Django
make createsuperuser   # Создать суперпользователя Django
make django-shell      # Открыть Django shell
make test              # Запустить тесты
make ps                # Показать статус контейнеров
make clean             # Остановить контейнеры и удалить volumes и образы
```

### Docker Compose

```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose down

# Просмотр логов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs -f backend
docker compose logs -f frontend
docker compose logs -f db
docker compose logs -f pgadmin

# Подключение к БД через psql
docker compose exec db psql -U postgres -d studyrepo

# Пересборка образов
docker compose up -d --build

# Выполнение команд в контейнере
docker compose exec backend python manage.py <команда>
docker compose exec backend python manage.py migrate
docker compose exec backend python manage.py createsuperuser
docker compose exec backend python manage.py shell

# Остановка и удаление всех контейнеров и volumes
docker compose down -v
```

### Django Management Commands

```bash
# Применение миграций
python manage.py migrate

# Создание миграций
python manage.py makemigrations

# Создание суперпользователя
python manage.py createsuperuser

# Сборка статических файлов
python manage.py collectstatic

# Django shell
python manage.py shell
```

### Frontend Commands

```bash
# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Просмотр собранного приложения
npm run preview

# Линтинг
npm run lint
```

## 🔧 Переменные окружения

### Backend

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `POSTGRES_DB` | Имя базы данных | `studyrepo` |
| `POSTGRES_USER` | Пользователь БД | `postgres` |
| `POSTGRES_PASSWORD` | Пароль БД | `postgres` |
| `POSTGRES_HOST` | Хост БД | `db` (в Docker) / `localhost` (локально) |
| `POSTGRES_PORT` | Порт БД | `5432` |
| `DJANGO_SECRET_KEY` | Секретный ключ Django | *(обязательно указать)* |
| `ALLOWED_HOSTS` | Разрешенные хосты | `localhost,127.0.0.1` |
| `CORS_ALLOWED_ORIGINS` | Разрешенные источники для CORS | `http://localhost:3000` |
| `DEBUG` | Режим отладки | `True` |

### Ports

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `BACKEND_PORT` | Порт бэкенда | `8000` |
| `FRONTEND_PORT` | Порт фронтенда | `3000` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `PGADMIN_PORT` | Порт pgAdmin | `5050` |
| `PGADMIN_EMAIL` | Email для pgAdmin | `admin@admin.com` |
| `PGADMIN_PASSWORD` | Пароль для pgAdmin | `admin` |

## 🗄️ Работа с базой данных

### Просмотр БД через pgAdmin (веб-интерфейс)

1. Откройте http://localhost:5050 в браузере
2. Войдите с учетными данными:
   - Email: `admin@admin.com` (или значение из `PGADMIN_EMAIL`)
   - Password: `admin` (или значение из `PGADMIN_PASSWORD`)
3. Добавьте новый сервер:
   - **Name:** StudyRepo DB (любое имя)
   - **Host:** `db` (имя сервиса в docker-compose)
   - **Port:** `5432`
   - **Username:** `postgres` (или значение из `POSTGRES_USER`)
   - **Password:** `postgres` (или значение из `POSTGRES_PASSWORD`)
   - **Database:** `studyrepo` (или значение из `POSTGRES_DB`)

### Просмотр БД через командную строку

```bash
# Подключиться к БД через psql
docker compose exec db psql -U postgres -d studyrepo

# Или используя Make
make shell-db
```

В psql вы можете выполнять SQL команды:
```sql
\dt              -- Показать все таблицы
\d table_name    -- Показать структуру таблицы
SELECT * FROM table_name;  -- Выбрать данные
\q               -- Выйти
```

### Просмотр БД через Django Admin

Django Admin также позволяет просматривать данные моделей:
- Откройте http://localhost:8000/admin/
- Войдите с учетными данными суперпользователя
- Выберите нужную модель для просмотра

## 🐛 Решение проблем

### Проблема: Контейнер не запускается

```bash
# Проверьте логи
docker compose logs <service_name>

# Пересоберите образы
docker compose up -d --build
```

### Проблема: Ошибка подключения к БД

- Убедитесь, что контейнер БД запущен: `docker compose ps`
- Проверьте переменные окружения в `.env`
- Убедитесь, что БД готова: `docker compose exec db pg_isready -U postgres`

### Проблема: Миграции не применяются

```bash
# Примените миграции вручную
docker compose exec backend python manage.py migrate
```

### Проблема: Статические файлы не загружаются

```bash
# Соберите статические файлы
docker compose exec backend python manage.py collectstatic --noinput
```

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

- **vladislav1029** - [GitHub](https://github.com/vladislav1029)

## 🤝 Вклад в проект

Вклад в проект приветствуется! Пожалуйста:

1. Сделайте Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте [Issue](https://github.com/vladislav1029/StudyRepo/issues) в репозитории.

---

**Приятной разработки! 🚀**

